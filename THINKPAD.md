# Thinkpad — Simplify personalweb to a pierre.computer-style plain-text site, keep the stipple load-in
<!-- pad:v1 -->

<!-- Edit this file by hand freely. `pad` reads and writes it; it never reformats
     prose sections. Task lines must keep the `- [ ] T1 · title` shape. -->

## Goal

srujanyamali.com becomes a static, framework-free multi-page site that looks and reads like https://pierre.computer: one plain-text column (JetBrains Mono 12px/20px, 40px padding, 480px measure), a three-line header with a blinking block cursor, `#`/`##` markdown-style headings, `- ` lists, `[label](url)` links where only the URL is underlined, no nav, no sidebar, no theme toggle (OS colour scheme only), monochrome except a Berkeley-gold selection. The home page (`index.html`) carries everything a recruiter needs (about, experience with dates, research, publications, ventures, open source, writing, email/GitHub/LinkedIn/résumé). The two research write-ups become article pages (`research/drosophila.html`, `research/redcarpet.html`) in pierre's article style with the real figures. The Cal stipple loading screen is kept on `/` and now ends by flying its dots onto the glyphs of the page text and dissolving into it, so the last frame of the intro is the first frame of the page. React, react-router, three.js/R3F, motion, the terrain, the 3D world, tailwind and postcss are gone. Old routes redirect. `npm run build` passes, and every page is verified by screenshot at desktop, phone and dark.

## Constraints

- Copy is in Srujan's voice: long flowing declaratives, claim then mechanism, no em dashes as punchline separators, never Moreover/Furthermore/Additionally. Reuse his existing sentences from src/data.js where possible; do not invent facts.
- Italic is reserved for species names (Drosophila, Staphylococcus aureus, S. aureus) and venue titles. Headings are roman.
- No framework, no runtime dependencies. Pages must render fully with JavaScript disabled (the intro is the only script, and only on /).
- Keep every PDF in public/ (old résumé links may be circulating). Never touch public/situationalawareness.pdf (user's untracked file).
- Never hardcode /Users/... paths in project files.

## Decisions
- 2026-09-22 · Static Vite MPA, no React: index.html, research/drosophila.html, research/redcarpet.html, 404.html — because pierre.computer is literally static HTML; content in the HTML survives no-JS, is readable in view-source, and drops ~300KB of JS
- 2026-09-22 · Remove the 3D world (/explore), terrain, wash transition, custom cursor, sidebar, nav, theme toggle — because user asked to simplify a lot like pierre and named only the loading screen as a keeper; git history keeps them
- 2026-09-22 · Intro handoff lands the dots on the rasterised glyphs of the visible page text, then the text fades in under them — because the terrain it used to land on is gone; landing on the words keeps the invariant that the intro's last frame is the page's first
- 2026-09-22 · Intro plays on / for fresh arrivals and reloads, not on in-site navigations or back/forward; ?intro forces, ?nointro skips, reduced-motion skips — because in an MPA every click back to home is a full load; replaying 7s each time would be a toll
- 2026-09-22 · Warm paper tones kept (#f4f3f0 / #0e0e0d, ink #121211 / #f2f1ed); selection is Berkeley gold #FDB515 — because his identity and the tuned stipple ink; gold selection is the one hidden accent, like pierre's yellow
- 2026-09-22 · Dates follow the linked June 2026 résumé (Visa Jan–May 2026, Mercor Aug 2025–Jan 2026), not the old data.js — because data.js was a year early on both; a recruiter compares the page to the PDF it links
- 2026-09-22 · Publications fold into the Research entries on home; full citations live on the article pages — because pierre's home is an index of links; the three presentations stay counted and cited


## Open questions

- [ ] Visa dates in data.js end May 2026 but the old landing copy said "I build LLM tooling at Visa" (present). New copy is tense-neutral; flag to user.

## Tasks

- [x] T1 · Port the Cal stipple load-in to vanilla JS and land its dots on the page text
      files: src/intro.js, src/intro.css, index.html
      accept: node /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/checks/intro_check.mjs
      notes: Frames that best show the handoff are in /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/T1/: - **Desktop:** f-3500 (outline, skip fading), f-4200 (a sheet of lines streaming in), f-4800 (dots landed as stippled words), f-5400 (crossfade), f-8500 (final page). - **2x crop:** z-4800.png, the landed dots up close; compare it to the same region of f-8500. - **Phone:** p-4100, p-4800, p-7000. - **Dark:** d-2500, d-4800, d-7000. - **Scrolled reload:** sr-landed.png. - **Resize re-plan:** ra-landed.png (resized to 420px wide during the assembly). Decisions and deviations the orchestrator should know: 1. **Dot count at the landing.** The outline's dots alone covered only about 40% of the glyph ink on desktop and about 20% on phone, so the words read as faint grey. At the settle each dot now splits into children that start exactly under their parent, up to 28k dots on desktop and 16k on phone. The outline itself still uses the 5000-dot mobile cap. This follows the reason for the cap (frame cost), not its letter: the settle is 1.7ms per frame on phone and 2.8ms on desktop in headless Chrome. If the cap should also apply to the landing, set LANDED_CAP_MOBILE = 5000, but phone text will then land sparse. 2. **Performance bug found and fixed.** A single path with thousands of exactly overlapping circles took about 1 second to fill on the first settle frame. The fix: paths are chunked at 1024 dots, children are not drawn until they leave their parent, and dots under 1px are drawn as squares of the same area. The fade is now CSS opacity on the canvas element, so overlapping dots fade as one layer. The only remaining 30–45ms frames are glyph sampling and pairing, and both fall in the static hold. 3. **Pairing dots to landing spots.** Both sets are sorted into rank bands by y, then by x within each band, instead of by angle. The outline shreds into rows and pours into the text column without dots crossing. Furthest-travel dots leave first, as in the old code. 4. **What gets rasterised besides plain text.** text-transform uppercase (the h2 text is mixed case in the source), the ::before marks (## and -), and link underlines. I measured Chrome's underline at baseline + text-underline-offset, and the gaps around descenders are copied too. Grey dates and ## marks land grey. 5. **Scroll lock and failsafe.** Scroll locks only on `html.intro-live`, which intro.js adds when it starts, so a module that never loads cannot trap scroll. That case is covered by a CSS failsafe that shows main at 4s; I verified it by blocking intro.js. The head also sets `scrollbar-gutter: stable` so the lock never reflows the text. 6. **Prefetch.** The head script starts `fetch('/stipple_data.json')` as `window.__introDots` as soon as it decides to play. If the data hasn't arrived after 2.5s, or the fetch fails, the intro skips straight to the page. 7. **Handled cases, all tested.** A width change during the settle quick-fades to the page; before the settle it re-lays the outline and re-plans the targets. Escape, Enter, a click anywhere and the skip button all work, and so does a scroll restored on reload. `pagehide` cleans up so a back/forward-cache restore isn't frozen mid-intro. A thrown error cleans up and logs to the console. 8. **Skip control.** Plain `[skip]` in 12px mono fg2, placed bottom-left at the text column's 40px inset (20px/32px on phone). It fades out when the settle starts, so the last frame contains only the page. 9. **Size and timing.** The built intro is 11.5 kB (5.3 kB gzipped). The whole intro takes about 5.8s from load.
      attempts: 1
      took: 26m
- [x] T2 · Write the two research articles in pierre article style with the real figures
      files: research/drosophila.html, research/redcarpet.html, src/site.css
      accept: node /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/checks/articles_check.mjs
      notes: (1) The title is `h2.top`, so it gets a "# " mark, which matches the home page's "# ABOUT" and puts "##" on the sections (PRESENTATIONS, FILES, FIGURES). Pierre uses "##" on its titles. (2) Additions beyond the brief, all sourced: a FILES section with each PDF plus a [Code] link to the GitHub repo from data.js projects; the verified page count and size on each PDF (poster 1 page, 2.3 MB; methods 21 pages, 13 MB); "Shao lab" in the Drosophila byline, taken from the old Landing.jsx copy; "Carpet Change Catcher." at the start of the methods note (the document's title in data.js, with its em dash dropped). (3) The article column is `main.wide` at 48em (80 columns). Captions sit on the line straight after the one-hairline frame, and figures are 2 lines apart. In dark mode images get `filter: brightness(0.9)` and are not inverted. Initials, surnames and "S. aureus" are joined with &nbsp;. (4) The home page publications list still breaks "R. / Oliver" across lines; T5 could add the same &nbsp; there. (5) public/work/redcarpet-summary.png isn't in data.js, so I left it out. (6) Puppeteer full-page screenshots show lazy images as blank white boxes. That comes from how the screenshot is taken, not from the pages. /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/T2/shoteager.mjs forces them to load first. Screenshots are in the same T2 folder.
      attempts: 1
      took: 26m
- [x] T3 · Delete the React app, write 404.html, rewrite vercel.json and _redirects for the static site
      files: 404.html, vercel.json, public/_redirects, src/App.jsx, src/main.jsx, src/pages, src/World3D.jsx
      accept: bash /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/checks/config_check.sh
      notes: - Before deleting, I grepped the repo (excluding node_modules, dist, .git). Nothing that stays imports the deleted files. The only hits were a comment in src/data.js mentioning src/icons.jsx, and the word "reduced-motion" in index.html. IntroAnimation.jsx imports only "react", which is harmless because nothing imports IntroAnimation.jsx, so Vite never bundles it. - URL fragments in redirects are kept (e.g. /experience -> /#experience). Vercel's docs say nothing about fragments, so I downloaded @vercel/routing-utils 6.6.0 into scratchpad and compiled this vercel.json with it. A redirect with no named segments passes the destination through unchanged, and the output was `{"src":"^/experience$","headers":{"Location":"/#experience"},"status":307}`. Browsers follow a fragment in a Location header. Cloudflare's _redirects docs say outright that destinations "can include fragments". In Netlify's parser a `#` only starts a comment at the beginning of a token, and `/#x` starts with `/`, so it is safe there too. - vercel.json has "cleanUrls": true and "trailingSlash": false, with no rewrites. It has 11 redirects, all `permanent: false` (307): /experience, /work and /cv go to /#experience; /projects to /#open-source; /ventures to /#ventures; /publications to /#publications; /research and /archive to /#research; /explore and /elsewhere to /; /resume to /Srujan_Yamali_SWE_Resume_June_2026.pdf. The compiled sources are exact matches like `^/research$`, so /research/redcarpet is not caught. Per the docs, cleanUrls serves research/redcarpet.html at /research/redcarpet and 308s the .html form to it. I left buildCommand and outputDirectory unset because the Vite preset defaults (vite build, dist) are correct. I also added `$schema`. - public/_redirects mirrors the same routes with 302 and has no catch-all. - The 404 page has no scripts and uses absolute URLs throughout, because Vercel serves it at whatever deep path was not found. It adds `<meta name="robots" content="noindex">`. The h2 gets its two-line top margin from an inline style (as pierre does), so the 404 doesn't depend on T2's article CSS and I didn't have to touch site.css. T5 may want to check that its spacing matches what T2 settles on. - The copy reads: "There is no page at this address. The site used to be spread over several pages that have since been folded into the home page, so whatever you were looking for is most likely there." - public/situationalawareness.pdf is copied into dist on every build, so it would go public with a CLI deploy. A git-based Vercel deploy leaves it out while it stays untracked. The user may want to know. - Left untouched: public/work/ (a directory in public/, contents not checked), the stale root dist/ from Aug 5, and the old `/research/:slug`-style content. There was no src/intro.css.
      attempts: 1
      took: 26m
- [x] T4 · Critique the new home page against pierre.computer and recruiter needs (read-only)
      files: /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/critique.md
      accept: test -s /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/critique.md
      notes: applied: résumé dates, grad year, SpaceX-first About, research mechanism restored, publications folded into Research glosses + article pages, ventures cut to 3 live domains, GitHub-only moved to Open source, Email me at, colophon 5 lines, 10ch rows, target=_blank, OG+canonical, 64px favicon, London ON Canada / Perth Australia. Skipped: impact numbers in About (keeps it pierre-basic; résumé carries them).
      attempts: 1
      took: 29m
- [x] T5 · Integrate: apply critique, remove IntroAnimation.jsx/data.js, build, full visual verify
      files: index.html, src/IntroAnimation.jsx, src/data.js
      accept: cd /Users/srujanyamali/Downloads/Coding/Websites/personalweb && npm run build && ! test -e src/data.js && ! test -e src/IntroAnimation.jsx
      deps: T1, T2, T3, T4
## Log

- 2026-09-22 · thinkpad created
- 2026-09-22 · dispatched wave: T1, T2, T3, T4
- 2026-09-22 · set T1 · files=src/intro.js, src/intro.css, index.html
- 2026-09-22 · wave 1 dispatched as workflow wf_d319f1f6-fec (T1 intro, T2 articles, T3 cleanup, T4 = 3 critics fidelity/recruiter/voice)
- 2026-09-22 · done T1 (attempt 1) · verified: `/Users/srujanyamali/Downloads/Coding/claude-tools/thinkpad/pad verify T1` gave "intro check OK", exit 0. The same check also passed against a production build (`vite build` into scratch, then `vite preview` on :5477, stopped afterwards). I ran the 11-frame introshots sequence from the brief, plus phone (390x844 at DPR 2), dark, 2x crops and edge cases, and read the PNGs. The landed frames line up exactly with the final page at desktop, phone, dark, a scrolled reload and after a reflowing resize. No console errors anywhere.
- 2026-09-22 · done T2 (attempt 1) · verified: `pad verify T2` printed "articles check OK" and exit 0. I screenshotted both pages full-page at 1440, at 390 and in dark, and read them: no horizontal overflow (scrollWidth = clientWidth), no image overflow on phone, and every gap between blocks measured as a whole line (0/20/40/60px). Home at 1440, 390 and dark is pixel-identical before and after my CSS change (PIL diff bbox None). A test build into the scratchpad succeeded and both built article pages have 0 scripts.
- 2026-09-22 · done T3 (attempt 1) · verified: `/Users/srujanyamali/Downloads/Coding/claude-tools/thinkpad/pad verify T3` printed "config check OK" and exited 0. I screenshotted http://localhost:5391/404.html at 1440, 390 and 1440 dark and read the PNGs in /private/tmp/claude-501/-Users-srujanyamali-Downloads-Coding-Websites-personalweb/7857fe0a-6842-4826-ba5d-ab3e2bb1d10a/scratchpad/T3/. The page matches pierre's article header: "SRUJAN YAMALI █", then "← BACK TO HOME", two blank lines, "# 404 NOT FOUND", then one paragraph. The block cursor is missing in the 800ms shots because they caught the off half of the blink; the 1200ms shot shows it. I also ran a probe Vite build of index.html and 404.html into scratchpad/T3/dist (the real `npm run build` cannot pass until T2 writes research/*.html). It emitted dist/404.html with an absolute hashed stylesheet (/assets/site-*.css), and public/_redirects was copied into dist.
- 2026-09-22 · done T4 (attempt 1) · verified: three critics (fidelity, recruiter, voice) returned ranked lists
- 2026-09-22 · done T5 · Integrate: apply critique, remove IntroAnimation.jsx/data.js, build, full visual verify
- 2026-09-22 · review wf_90ad380a-50f: 11 confirmed / 15 refuted; fixed WebKit overlay race (0/20 broken after), Tab/Enter focus, Stryda→www, Thoughts removed, 4 research caption/claim corrections, <wbr> in URLs; slides PDF page 1 left for user
